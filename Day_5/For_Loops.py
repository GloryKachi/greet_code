if __name__ == '__main__':
    cohort_names = ["Illuminaries", "Unicorns", "Paragons", "Regnos"]
    for cohort in cohort_names:
        print(cohort + " is the name of a cohort in semicolon")

print(f"{cohort_names[-1]}!")
